<script>
    function lang_datatable() {
        return {
            processing: "{{ __('app.processing') }}",
            lengthMenu: "{{ __('app.lengthMenu') }}",
            zeroRecords: "{{ __('app.zeroRecords') }}",
            info: "{{ __('app.info') }}",
            infoEmpty: "{{ __('app.infoEmpty') }}",
            infoFiltered: "{{ __('app.infoFiltered') }}",
            search: "{{ __('app.search') }}",
            paginate: {
                first: "{{ __('app.paginate.first') }}",
                previous: "{{ __('app.paginate.previous') }}",
                next: "{{ __('app.paginate.next') }}",
                last: "{{ __('app.paginate.last') }}"
            }
        };
    }
</script>
